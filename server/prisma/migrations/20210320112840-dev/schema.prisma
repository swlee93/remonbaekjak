generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model Task {
  id       Int    @id @default(autoincrement())
  name     String
  type     String
  description String
  createdBy String
  createdAt Int
}
